{
  "log_io": {
    "enabled": true,
    "log_dir": "logs/io",
    "request_file_pattern": "{run_name}__{attempt}__request.json",
    "response_file_pattern": "{run_name}__{attempt}__response.json"
  },

  "runs": [
    {
      "name": "generate_class_headers",
      "profile_file": "context_files/profiles/openai/class_header_generator.json",
      "task_description": "Generate Python import header statements for all classes defined in the manifest.json.",
      "context_file": [
        "expense_tracker/manifest/manifest.json"
      ],
      "target_file": "expense_tracker/headers/class_imports.txt",
      "allowed_actions": ["file_write"],
      "retry": 0,
      "agent_input": {
        "notes": "Take every class defined in manifest.modules[*].components[*] and output deterministic import statements."
      }
    }
  ]
}
