# === CONTEXT START ===
# This module handles rendering of the game state for a Tetris game.
# It includes the Renderer class with a method to render the current state of the game board.
# === CONTEXT END ===

class Renderer:
    def __init__(self, width, height):
        """
        Initialize the renderer with the dimensions of the game board.

        :param width: The width of the game board.
        :param height: The height of the game board.
        """
        self.width = width
        self.height = height

    def render_board(self, board):
        """
        Render the current state of the game board.

        :param board: A 2D list representing the game board, where each element is a cell.
        """
        for row in board:
            print(''.join(row))

# Example usage:
# renderer = Renderer(10, 20)
# game_board = [[' ' for _ in range(10)] for _ in range(20)]
# renderer.render_board(game_board)
