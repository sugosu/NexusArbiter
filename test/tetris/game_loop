# === CONTEXT START ===
# This module controls the main game loop for a Tetris game.
# It includes the GameLoop class with an is_running field.
# The start method manages the game loop and state transitions.
# === CONTEXT END ===

class GameLoop:
    def __init__(self):
        self.is_running = False

    def start(self):
        """
        Starts the game loop, managing the game state transitions.
        """
        self.is_running = True
        while self.is_running:
            # Placeholder for game loop logic
            # Process input, update game state, render output
            pass

    def stop(self):
        """
        Stops the game loop.
        """
        self.is_running = False
